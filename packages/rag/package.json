{
  "name": "@corpus-lens/rag",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc -b",
    "test": "tsc -p tsconfig.test.json && vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@corpus-lens/shared": "workspace:*",
    "gpt-tokenizer": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
