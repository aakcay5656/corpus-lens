/**
 * Scaffold placeholder. It exists so the workspace compiles and so a cross-package
 * import can be proven end to end before any real module lands.
 *
 * Replaced in Step 3 by the Zod contracts that define the API boundary.
 */
export const PACKAGE_NAME = "@corpus-lens/shared";
