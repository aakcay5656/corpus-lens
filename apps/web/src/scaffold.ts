import { PACKAGE_NAME as SHARED_PACKAGE } from "@corpus-lens/shared/package-info";

/**
 * Scaffold placeholder. Next.js, Tailwind and the App Router tree arrive in Step 10.
 *
 * The import proves the browser-side workspace consumes the same contract package the
 * API validates against, which is the point of putting the Zod schemas in a package.
 */
export const CONTRACT_SOURCE = SHARED_PACKAGE;
